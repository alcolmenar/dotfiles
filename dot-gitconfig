[user]
	name = Al Colmenar
	email = aljcolmenar@gmail.com
	signingkey = ~/.ssh/signing_key.pub
[commit]
	gpgsign = true
[init]
	defaultBranch = main
[push]
	autoSetupRemote = true
[gpg]
	format = ssh
[core]
  editor = nvim
  pager = delta
	excludesfile = /Users/al.colmenar/.gitignore_global
[interactive]
  diffFilter = delta --color-only
[delta]
  features = collared-trogon
  navigate = true    # use n and N to move between diff sections
  light = false      # set to true if you're in a terminal w/ a light background color (e.g. the default macOS terminal)
  line-numbers = true
  side-by-side = true
[merge]
  conflictstyle = diff3
[diff]
  colorMoved = default

[include]
  path = ~/.config/delta/themes.gitconfig

[includeIf "hasconfig:remote.*.url:**github.com?score[mb]*/**"]
  path = ~/.gitconfig_thescore

[includeIf "hasconfig:remote.*.url:**github.com?penn-interactive/**"]
  path = ~/.gitconfig_penn
